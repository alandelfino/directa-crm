---
description: Debug errors
---

Rode o 'npm run build', verifique os erros do terminal e corrija os erros um por um, quando terminar de corrijir, rode 'tsc', verifique os erros do terminal e corrija os erros um por um, quando terminal, rode 'npx eslint', verifique o terminal e corrija os erros do terminal um por um.